# FitCrew Challenge — database directory is never web-addressable.
# The migration runner also independently enforces CLI-only execution.

Options -Indexes

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
