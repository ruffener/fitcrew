# Tests

Phase 1 skeleton does not include automated tests yet.

Future smoke tests should verify:

- Bootstrap loads.
- `.env` config is read safely.
- Public routes render.
- Protected app route redirects until auth is implemented.
- No product behavior is accidentally exposed before Governance approval.
